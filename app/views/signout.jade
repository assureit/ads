li.line.dropdown
    a.dropdown-toggle(href="#", data-toggle="dropdown")
        span.glyphicon.glyphicon-user
        |&nbsp;
        = userName
        strong.caret
    .dropdown-menu
        form#sign-in-form.navbar-form(method="post", action="#{basepath}/logout")
            input.btn.btn-danger(type="submit", style="width: 100%; height: 32px; font-size: 13px;", value="Sign out")
