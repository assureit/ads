DFault PortScanMonitor() {
	println("PortScanMonitor called...");
	DFault ret = null;
	if (Monitor) {
		//pass
		println("success");
	}
	else {
		println("fault!!");
		ret = fault(Risk);
	}
	return ret;
}
