DFault BlockIP() {
	println("BlockIP called...");
	DFault ret = null;
	command rec, sleep;
	int i = 0;
	while(i < 5) {
		println(" *");
		sleep 1;
		i += 1;
	}
	rec -m pushRawData -l ServerA -t IsPortScaned -d 0;
	return ret;
}
